def project_hidden_states(hidden_states: List[np.array], method: str = "umap") -> np.array:
    # Stack all hidden states into a matrix
    # Run PCA or UMAP
    # Return the projected 2D/3D coordinates
    return projected_coords
